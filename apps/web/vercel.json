{
	"framework": "astro",
	"buildCommand": "pnpm --filter @mcp-sentry/web build",
	"installCommand": "pnpm install --frozen-lockfile",
	"outputDirectory": "apps/web/dist",
	"ignoreCommand": "git diff --quiet HEAD^ HEAD -- apps/web packages/cli/src/checks pnpm-lock.yaml"
}
